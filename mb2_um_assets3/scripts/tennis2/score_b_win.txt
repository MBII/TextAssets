//Generated by BehavEd

rem ( "BLU Win" );
if ( $get( FLOAT, "redBlock")$, $=$, $0$ )
{
	use ( "block_r" );
	set ( "redBlock", $5$ );
}
else (  )
{
	set ( "redBlock", $5$ );
}
use ( "holo_b" );
use ( "sound_win" );
use ( "target_b" );
wait ( 1000.000 );
use ( "elec_b" );
wait ( 12000.000 );
use ( "obj_b" );

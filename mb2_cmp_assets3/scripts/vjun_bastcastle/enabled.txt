//Generated by BehavEd

rem ( "BOMB ENABLED" );
declare ( /*@DECLARE_TYPE*/ FLOAT, "bomb_status" );
set ( "bomb_status", $1$ );
use ( "tick_starter" );
use ( "bombplace" );
use ( "bombready" );
use ( "act_mess" );

affect ( "bombtimer", /*@AFFECT_TYPE*/ FLUSH )
{
	set ( "SET_INACTIVE", "false" );
}

use ( "bombtimer" );
